var uploader = $('#<%= @uploader_id %>');

if (uploader.hasClass("gallery-uploader")) {
  $('#<%= @uploader_id %>').find('.uploader-data-wrapper').append('<%= j render partial: "assetable/assets/asset", locals: { asset: @asset, fieldname: @fieldname } %>');
} else {
  $('#<%= @uploader_id %>').find('.uploader-data-wrapper').html('<%= j render partial: "assetable/assets/asset", locals: { asset: @asset, fieldname: @fieldname } %>');
};